<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">
    <item
        android:id="@+id/action_save_search"
        android:icon="@android:drawable/ic_menu_search"
        android:orderInCategory="1"
        android:title="@string/action_save_search"
        app:showAsAction="never" />
    <item
        android:id="@+id/action_view_saved_jobs"
        android:icon="@android:drawable/ic_menu_search"
        android:orderInCategory="2"
        android:title="@string/action_view_saved_jobs"
        app:showAsAction="never" />
    <item
        android:id="@+id/action_restore_jobs"
        android:icon="@android:drawable/ic_menu_search"
        android:orderInCategory="3"
        android:title="@string/action_restore"
        app:showAsAction="never" />

</menu>
